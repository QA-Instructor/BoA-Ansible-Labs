events {}

http {
    upstream appservers {
        server 35.211.130.212:8080; # <- add your IPs here
        server 35.211.10.250:8080; # <- and here
    }
    server {
        listen 80;
        location / {
            proxy_pass http://appservers;
        }
    }
}